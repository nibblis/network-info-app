# Демонстрация отладки и запуска приложения

Этот документ объясняет, как настроить, запустить и отладить Android-приложение в IDE.

### 1. Настройка схем/профилей (Build Types)

В Android для управления сборками используются **Build Types**. Обычно их как минимум две: `debug` ( для разработки) и `release` (для публикации). Они позволяют настроить приложение по-разному для каждой версии.

Например, можно задать разные базовые URL для сети.

**Где настраивать:** `app/build.gradle.kts`

```kotlin
android {
    // ...
    buildTypes {
        getByName("debug") {
            // Добавляем поле в BuildConfig, которое будет доступно только в debug-сборке
            buildConfigField("String", "BASE_URL", "\"https://api.debug.example.com/\"")
            isMinifyEnabled = false
        }
        getByName("release") {
            // Для release-сборки используем продуктовый URL
            buildConfigField("String", "BASE_URL", "\"https://api.prod.example.com/\"")
            isMinifyEnabled = true
            proguardFiles(
                getDefaultProguardFile("proguard-android-optimize.txt"),
                "proguard-rules.pro"
            )
        }
    }
}
```

Теперь в коде можно обращаться к `BuildConfig.BASE_URL`, и он будет разным в зависимости от типа сборки.

### 2. Запуск на эмуляторе/устройстве

1.  **Выбор устройства**: В верхней панели Android Studio выберите эмулятор или подключенное физическое устройство из выпадающего списка.
2.  **Выбор Build Variant**: В левой панели откройте вкладку **Build Variants**. Выберите нужный вариант, например, `app:debug`.
3.  **Запуск**: Нажмите на зеленую кнопку **Play** (▶️) для обычной установки и запуска, или на иконку жука (🐞) для запуска в режиме отладки.

### 3. Процесс отладки

#### Логи (Logcat)

Самый простой способ отладки. Добавьте логи в свой код и отслеживайте их в окне **Logcat**.

**Пример в коде:**

```kotlin
// OrganizationRepository.kt
import android.util.Log

suspend fun refreshOrganizations() {
    try {
        Log.d("NetworkApp", "Fetching fresh data from network...")
        val freshData = api.getOrganizations()
        dao.insertAll(freshData)
        Log.i("NetworkApp", "Data refreshed successfully!")
    } catch (e: Exception) {
        Log.e("NetworkApp", "Failed to refresh data", e) // Логируем ошибку
    }
}
```

**Как использовать Logcat:**
1.  Откройте окно **Logcat** (внизу IDE).
2.  В строке поиска введите фильтр, например, `package:mine tag:NetworkApp`, чтобы видеть только логи вашего приложения с нужным тегом.
3.  Следите за сообщениями. Ошибки (`Log.e`) будут подсвечены красным.

#### Точки останова (Breakpoints)

Breakpoint останавливает выполнение кода в указанной строке, позволяя изучить текущее состояние программы.

**Как использовать:**
1.  Кликните левой кнопкой мыши на полях слева от нужной строки кода, чтобы поставить красную точку (breakpoint).
2.  Запустите приложение в режиме отладки (🐞).
3.  Когда выполнение дойдет до этой точки, приложение "замрет", а в IDE откроется окно **Debug**.
4.  В этом окне вы можете:
    -   **Смотреть переменные**: Увидеть значения всех локальных и глобальных переменных в момент остановки.
    -   **Выполнять код по шагам**: Использовать кнопки `Step Over` (F8), `Step Into` (F7), `Resume` (F9).
    -   **Вычислять выражения**: Использовать `Evaluate Expression` (Alt+F8) для выполнения произвольного кода в текущем контексте.

#### Инспектор базы данных (Database Inspector)

Позволяет смотреть содержимое вашей Room базы данных в реальном времени.

**Как использовать:**
1.  Запустите приложение на устройстве с API 26+.
2.  Откройте **View > Tool Windows > App Inspection**.
3.  Выберите вкладку **Database Inspector**.
4.  В левой панели вы увидите свои таблицы (`organizations`). Кликнув на таблицу, вы увидите ее содержимое.
5.  Вы даже можете изменять данные прямо в инспекторе или выполнять SQL-запросы для отладки.

